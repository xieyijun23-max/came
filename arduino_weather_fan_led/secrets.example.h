#pragma once

// Copy this file to secrets.h, then fill in your real router details.
// Keep secrets.h private; it is ignored by git.
#define WIFI_SSID "your_2_4ghz_wifi_name"
#define WIFI_PASSWORD "your_wifi_password"

// Optional: if your router channel is fixed, document it here so ESP-NOW-only
// receiver nodes can be set to the same channel when needed.
#define WIFI_CHANNEL 6
